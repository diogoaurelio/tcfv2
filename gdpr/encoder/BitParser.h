//
// Created by diogo on 17.02.20.
//

#ifndef TCF_BITPARSER_H
#define TCF_BITPARSER_H


#include <vector>
#include <cstdint>
#include <string>

/*
class BitParser {
private:
    std::size_t bit_index = 0;
    std::vector<uint8_t> bytes;

public:
    explicit BitParser(const std::string& val);

    size_t remaining() const;

    void skip(size_t bit_count);

    uint64_t operator()(size_t bit_count);

};
*/

#endif //TCF_BITPARSER_H
